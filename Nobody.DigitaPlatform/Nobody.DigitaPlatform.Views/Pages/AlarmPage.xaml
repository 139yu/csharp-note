<UserControl x:Class="Nobody.DigitaPlatform.Views.Pages.AlarmPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Nobody.DigitaPlatform.Views.Pages"
             mc:Ignorable="d"
             DataContext="{Binding Source={StaticResource Locator},Path=AlarmViewModel}"
             d:DesignHeight="450" d:DesignWidth="1000">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="pack://application:,,,/Nobody.DigitaPlatform.Assets;component/Styles/ButtonStyle.xaml" />
                <ResourceDictionary
                    Source="pack://application:,,,/Nobody.DigitaPlatform.Assets;component/Styles/ComboxStyle.xaml" />
                <ResourceDictionary
                    Source="pack://application:,,,/Nobody.DigitaPlatform.Assets;component/Styles/DataGridStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="TextBox">
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border MinHeight="26" Name="br" BorderThickness="1" CornerRadius="5 " BorderBrush="#eee"
                                    Background="#dfff">
                                <Grid>
                                    <TextBlock Text="{TemplateBinding Tag}" Margin="5,0,0,0"
                                               VerticalAlignment="Center" Name="tag" FontSize="10"
                                               Visibility="Collapsed" Foreground="#aaa" />
                                    <ScrollViewer Name="PART_ContentHost" Margin="4,0" />
                                    <TextBlock VerticalAlignment="Center" FontSize="12" Foreground="#aaa"
                                               HorizontalAlignment="Right" Margin="0,0,5,0" Text="&#xe647;"
                                               FontFamily="{StaticResource Iconfont}" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="#16a1ff" TargetName="br"></Setter>
                                </Trigger>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self},Path=Text}"
                                             Value="">
                                    <Setter TargetName="tag" Property="Visibility" Value="Visible"></Setter>
                                </DataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="TextBlock" x:Key="CenterStyle">
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Border Height="15" Width="3" CornerRadius="2" HorizontalAlignment="Left" Background="Gold" />
        <TextBlock FontWeight="Normal" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Left"
                   Margin="5,0,0,0"
                   Text="实时报警状态" Foreground="#AA409EFE" />
        <StackPanel Orientation="Horizontal" TextBlock.FontSize="12" HorizontalAlignment="Right">
            <TextBlock Margin="5,0,0,0" Text="关键词" Foreground="#888" VerticalAlignment="Center" />
            <TextBox VerticalAlignment="Center" Width="260" Margin="5,0,0,0"
                     Tag="输入设备ID/设备名称/变量ID/变量名称检索" />
            <Button Margin="5,0,0,0" Style="{StaticResource InfoButton}" Width="60" VerticalAlignment="Center"
                    Content="刷新" />
            <Button Margin="5,0,0,0" Command="{Binding ExportExcelCommand}"
                    Style="{StaticResource PrimaryButton}" Width="150">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock VerticalAlignment="Center" Margin="0,0,5,0" FontFamily="{StaticResource Iconfont}"
                               Text="&#xe60e;" />
                    <TextBlock Text="导出报警列表"></TextBlock>
                </StackPanel>
            </Button>
        </StackPanel>

        <Border Background="#efff" Grid.Row="1" CornerRadius="0,0,5,5"/>
        <DataGrid Grid.Row="1" Background="Transparent" IsReadOnly="True" ItemsSource="{Binding Alarms}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="序号" Width="40" Binding="{Binding Index}" Foreground="#AAA" ElementStyle="{StaticResource CenterStyle}"/>
                <DataGridTextColumn Header="报警信息" Width="1.5*" Binding="{Binding AlarmContent}" ElementStyle="{StaticResource CenterStyle}" />
                <DataGridTextColumn Header="报警等级" Width="60" Binding="{Binding Level}" ElementStyle="{StaticResource CenterStyle}" FontWeight="Normal"/>
                <DataGridTextColumn Header="设备编号" Width="auto" Binding="{Binding DNum}" Foreground="#AAA"/>
                <DataGridTextColumn Header="设备名称" Width="*" Binding="{Binding DeviceName}" ElementStyle="{StaticResource CenterStyle}"/>
                <DataGridTextColumn Header="变量编号" Width="auto" Binding="{Binding VNum}" Foreground="#AAA"/>
                <DataGridTextColumn Header="变量名称" Width="*" Binding="{Binding VarName}" ElementStyle="{StaticResource CenterStyle}"/>
                <DataGridTextColumn Header="记录值" Width="60" Binding="{Binding AlarmValue}" ElementStyle="{StaticResource CenterStyle}"/>
                <DataGridTextColumn Header="记录时间" Width="120" Binding="{Binding DateTime}" ElementStyle="{StaticResource CenterStyle}" Foreground="#AAA"/>
                <DataGridTextColumn Header="处理时间" Width="120" Binding="{Binding SolveTime}" ElementStyle="{StaticResource CenterStyle}" Foreground="#AAA"/>
                <DataGridTextColumn Header="记录人" Width="80" Binding="{Binding UserName}" ElementStyle="{StaticResource CenterStyle}" Foreground="#AAA"/>
                <DataGridTextColumn Header="当前状态" Width="60" Binding="{Binding StateName}" ElementStyle="{StaticResource CenterStyle}" FontWeight="Normal"/>
                <DataGridTemplateColumn Header="操作" Width="70">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Hyperlink TextDecorations="None" 
                                               Command="{Binding Path=CancelAlarmCommand}">处理</Hyperlink>
                                </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>